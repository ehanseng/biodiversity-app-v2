# Configuración para servidor remoto MySQL
# Copia este archivo a .env y modifica con tus datos reales

# === CONFIGURACIÓN SERVIDOR REMOTO ===
# Cambia estos valores por los de tu servidor
DB_HOST=tu-servidor-mysql.com
DB_PORT=3306
DB_USER=tu_usuario_mysql
DB_PASSWORD=tu_password_mysql
DB_NAME=biodiversity_db

# Puerto del servidor backend
PORT=3001

# JWT Secret (usa uno seguro en producción)
JWT_SECRET=tu_jwt_secret_muy_seguro_y_largo_para_produccion

# Configuración de archivos
UPLOAD_PATH=uploads/
MAX_FILE_SIZE=5242880

# === CONFIGURACIÓN CORS ===
# Agrega las URLs de tu frontend en producción
CORS_ORIGINS=http://localhost:8081,https://tu-dominio.com

# === CONFIGURACIÓN SSL (opcional) ===
# Si tu servidor MySQL requiere SSL
DB_SSL=false
# DB_SSL_CA_PATH=/path/to/ca-cert.pem
# DB_SSL_KEY_PATH=/path/to/client-key.pem
# DB_SSL_CERT_PATH=/path/to/client-cert.pem
